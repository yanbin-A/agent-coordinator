# Security Policy

Do not commit secrets, API keys, tokens, private handoff logs, runtime state, local paths, or personal data.

If you find a sensitive-data issue, rotate any affected credentials immediately and remove the data from history before publishing again.
